"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const swagger_ui_express_1 = __importDefault(require("swagger-ui-express"));
const swagger_jsdoc_1 = __importDefault(require("swagger-jsdoc"));
const Runner_1 = require("./runners/Runner");
/**
 * Implicit Express Server.
 */
class App {
    /**
     * Initialize and Setup the server.
     * @param config ConfigMetadataStorage
     */
    init(config) {
        this.app = config.expressApplication;
        const swaggerDefinition = !config.relativePath ? config.swaggerDefinitionFilePath : process.cwd() + '/' + config.swaggerDefinitionFilePath;
        this.run(swaggerDefinition, config.swaggerEndPointUrl);
    }
    /**
     * Serves swagger file in specified file and endpoint
     * @param swaggerEndPoint  swaggerEndPointUrl
     * @param swaggerDefinitionFile swaggerDefinitionFilePath
     */
    serveSwagger(swaggerDefinitionFile, swaggerEndPoint) {
        Promise.resolve().then(() => __importStar(require(swaggerDefinitionFile))).then((file) => {
            const specs = (0, swagger_jsdoc_1.default)(file);
            this.app.use(swaggerEndPoint, swagger_ui_express_1.default.serve, swagger_ui_express_1.default.setup(specs));
        });
    }
    /**
     * Runs and executes swaggiffy
     * @param swaggerEndPoint  swaggerEndPointUrl
     * @param swaggerDefinitionFile swaggerDefinitionFilePath
     */
    async run(swaggerDefinitionFile, swaggerEndPoint) {
        Runner_1.Runner.execute();
        this.serveSwagger(swaggerDefinitionFile, swaggerEndPoint);
    }
}
exports.default = App;
//# sourceMappingURL=app.js.map