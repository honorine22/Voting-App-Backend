import * as express from 'express';
import { APIDefinitionOptions } from '../typings';
/**
 * Create swagger path definition
 * @param router Express router
 * @returns apiPathDefinitions {APIPathDefinition}
 */
export declare function registerDefinition(router: express.Router, options: APIDefinitionOptions): void;
